<div id="legend">
    <a href="/docs/cameras" class="bs-callout bs-callout-danger">
        Home
    </a> &nbsp; 
    <% Object.keys(pageData).forEach(function(v,n){
        var callout_color='warning'
        if(v.indexOf('rtsp')>-1){
            callout_color='info'
        }
    %>
<% Object.keys(pageData[v].types).forEach(function(b){
        var block_title=b;
        if(b.indexOf('FFMPEG')>-1){
            block_title='H.264'
        }
    %>
    <a href="#<%-block_title+v%>" class="bs-callout bs-callout-<%-callout_color%>">
        <%-block_title%> <small>over <%-v%></small>
    </a> &nbsp;                                
        <%}) %>
    <% }) %>
</div>
<div>
    <% Object.keys(pageData).forEach(function(v,n){
        var callout_color='warning'
        if(v.indexOf('rtsp')>-1){
            callout_color='info'
        }
    %>
<% Object.keys(pageData[v].types).forEach(function(b){
        var block_title=b;
        if(b.indexOf('FFMPEG')>-1){
            block_title='H.264'
        }
    %>
    <div id="<%-block_title+v %>" class="section-block bs-callout bs-callout-<%-callout_color%>">

        <div class="row">
            <div class="col-md-6">
                <h4><%-block_title%> <small>over <%-v%></small></h4>
                <table class="table"><% pageData[v].types[b].forEach(function(m){%><tr><td><%-m%></td></tr><%}) %></table></div>
            <div class="col-md-6">
                <h5>Models &nbsp; <small>Use the search bar to quickly find models</small></h5>
                <% parsedModels=[];
                pageData[v].models.forEach(function(m){ 
                    if(m.toLowerCase().indexOf('other')>-1){return}
                    parsedModels.push(m)
                }) %>
                <%- parsedModels.join(', ') %>
            </div>
        </div>

    </div>
            <%}) %>
    <% }) %>
</div>